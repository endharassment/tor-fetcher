version: 2.1

orbs:
  go: circleci/go@4.0.0

jobs:
  build:
    docker:
      - image: cimg/go:1.26
    resource_class: large
    steps:
      - checkout
      - go/mod-download-cached
      - run:
         command: go install ./...
      - run:
         command: go test ./...
